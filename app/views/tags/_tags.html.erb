<% tags ||= {} %>
<% channel ||= OpenStruct.new(uid: "") %>

<% tags.each do |tag_title, channels_count| %>
    <%= render partial: "tags/tag", locals: {channel: channel, tag_title: tag_title, channels_count: channels_count} %>
<% end %>